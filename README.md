# 情绪零点 (Emotion Lite)

一个极致轻量、保护隐私的情绪垃圾桶应用。支持多端同步，数据掌握在你自己手中。

## 🌟 特性

- **隐私优先**：默认不记录任何内容，"阅后即焚"。
- **轻量化**：纯静态网页应用 (PWA)，可安装到手机和电脑。
- **多端同步**：支持 **WebDAV** (如坚果云)，可同步你的成就统计和设置。
- **免费部署**：完全免费，无需服务器。

## 🚀 极速部署指南 (三选一)

### 方案 A：Vercel (推荐，速度最快)
1. 将本项目上传到你的 GitHub。
2. 访问 [Vercel.com](https://vercel.com)，使用 GitHub 登录。
3. 点击 **Add New...** -> **Project**。
4. 选择刚才上传的 `emotion-lite` 仓库。
5. 点击 **Deploy**。
6. 等待 1 分钟，你会获得一个 `https://xxx.vercel.app` 的永久免费地址。

### 方案 B：GitHub Pages (完全免费)
1. 在 GitHub 上创建一个新仓库。
2. 将本项目代码推送到该仓库。
3. 进入仓库 **Settings** -> **Pages**。
4. 在 **Source** 选择 `GitHub Actions`。
5. 等待 Actions 跑完（约 2 分钟），即可访问。

### 方案 C：Netlify Drop (无需账号，拖拽部署)
1. 在本地运行 `npm run build`，会生成一个 `dist` 文件夹。
2. 访问 [app.netlify.com/drop](https://app.netlify.com/drop)。
3. 将 `dist` 文件夹直接拖进去。
4. 立即获得访问地址。

## ☁️ 如何开启多端同步 (以坚果云为例)

1. **注册坚果云账号**。
2. 登录网页版，点击右上角账户 -> **安全选项**。
3. 在 **第三方应用管理** 中，点击 "添加应用密码"，名称填 `情绪零点`。
4. 打开本应用，点击右上角 **设置**。
5. 填写：
   - **服务器地址**: `https://dav.jianguoyun.com/dav/`
   - **账号**: 你的坚果云注册邮箱
   - **密码**: 刚才生成的应用密码
6. 点击 **保存配置** 和 **立即同步**。

## 🛠️ 本地运行

```bash
git clone https://github.com/your-username/emotion-lite.git
cd emotion-lite
npm install
npm run dev
```

## 📄 许可证

MIT
